
1. 项目管理增加一个字段：项目负责人，关联用户列表，就数据默认是admin
2. 接口详情页和用例详情页冗余显示的接口地址，请显示完整，把引用的服务地址
也拼接起来显示，这样方便排查

1. 接口参数支持上传文件，新增和编辑都需要增加，参考postman
2. 实现JWT认证，用户登录成功后，返回token，用户每次请求非登录接口时，携带token，服务端验证token，并返回用户信息







